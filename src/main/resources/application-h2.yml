spring:
  datasource:
    #url: jdbc:h2:mem:mosaicdb
    url: jdbc:h2:file:./data/h2
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

server:
  port: 8082 
  servlet:
    context-path: /api